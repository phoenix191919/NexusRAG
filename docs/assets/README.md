# Docs assets

Drop portfolio screenshots here (e.g. `graph-ui.png`, `chat-citations.png`) and reference them from the root README:

```markdown
![Knowledge map](docs/assets/graph-ui.png)
```

Keep images under ~1–2 MB; prefer PNG/WebP. Do not commit PDFs or `.env` files.
